import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';
import {functions} from "./functions/functions";

/*
test('renders learn react link', () => {
  render(<App />);
  const linkElement = screen.getByText(/something/i);
  expect(linkElement).toBeInTheDocument();

});

test("wurzel",()=>{
  expect(functions.wurzel(3)).toBeGreaterThan(1);
});

test("hier teste ich die Wurzel",() =>{
  expect(functions.wurzel(4)).toBe(3);
})

test("ich teste auf 0 ", () => {
  expect(functions.wurzel(0)).toBe(0);
})*/