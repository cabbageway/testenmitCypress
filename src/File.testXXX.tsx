import {functions} from "./functions/functions";
import {functions as myfunctions}  from "./functions/arrays"
//
// import {functions as myfetch} from "./functions/apis"
/*
test ("das möchte ich testen z.B. 2+2 =4", () => {
    expect (functions.add(2,4)).toBe(6)
});
test ("teste else Zweig", () => {
    expect (functions.add(0,4)).toBe(4)
});
*/

/* Schlüsselwort test ('String was der Test soll', () =>
{ expect (woher.funktionsZutesten(Parameter).Methode(Vergleichswert)
   )};  */

// Bitte für Sub und Division auch durchführen

// Function soll Null zurückliefern
/*
test ("Test ob die function null liefert", () => {
    expect(functions.isNull()).toBeNull();
})
*/
//

// Aufgabe schreibt eine Funktion die ein Objekt aus einem Array zurückliefert und es soll getestet werden
// ob genau 1 zurückgeliefert wird
// ob genau ein bestimmtes raus kommt
// welche Testfälle sind alle zu machen


