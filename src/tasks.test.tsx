import {TodoList} from "./functions/tasks";

beforeAll(() => {

})
/*
test("Teste den Aufruf der Klasse", () => {
    const myTasks = new TodoList();
    expect(new TodoList()).not.toBeNull()
})


test("Einfügen eines Tasks", () => {
    const myTasks = new TodoList();
    myTasks.addTask("arbeiten")
    expect(myTasks.tasks.length).toBeGreaterThan(0);

})







test ("Markieren als erledigt", () => {
    const myTasks = new TodoList();
    myTasks.addTask("arbeiten");
    myTasks.markTaskAsDone("arbeiten");
    expect(myTasks.tasks[0].done).toBe(true);
})

test ("Löschen eines Tasks", () => {
    const myTasks = new TodoList();
    myTasks.addTask("arbeiten");
    myTasks.removeTask("arbeiten");
    expect(myTasks.tasks.length).toBe(0);
})
*/

/* Erstelle eine zweite Liste global, in der mehrere Tasks enthalten sind */
/* Schreibe dann die Tests  für diese Fälle */